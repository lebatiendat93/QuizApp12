<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="@drawable/bg_ripple_gray_solid_white_r20"
    android:padding="8dp">

    <ImageView
        android:id="@+id/img_subject"
        android:layout_width="64dp"
        android:layout_height="64dp"
        android:src="@drawable/ic_faculty_of_science" />

    <androidx.appcompat.widget.AppCompatTextView
        android:id="@+id/tv_subject_name"
        style="@style/style_text_medium_size_16"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginStart="16dp"
        android:layout_marginTop="8dp"
        android:layout_toRightOf="@id/img_subject"
        tools:text="Khoa tự nhiên" />

    <androidx.appcompat.widget.AppCompatTextView
        android:id="@+id/tv_exam_number"
        style="@style/style_text_regular_size_12"
        android:layout_width="110dp"
        android:layout_height="wrap_content"
        android:layout_below="@id/tv_subject_name"
        android:layout_marginStart="16dp"
        android:layout_marginTop="6dp"
        android:layout_toRightOf="@id/img_subject"
        android:ellipsize="end"
        android:maxLines="1"
        tools:text="Khoa tự nhiên 100 đề" />

    <ImageView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignTop="@id/img_subject"
        android:layout_alignBottom="@+id/img_subject"
        android:layout_alignParentEnd="true"
        android:paddingHorizontal="16dp"
        android:paddingEnd="8dp"
        android:src="@drawable/ic_arrow" />

</RelativeLayout>